#!/bin/bash

# Activate the virtual environment (assuming it's named venv and in the same directory)
source venv/bin/activate

# Run the Python application using the existing run.py
echo "Starting backend server..."
python run.py
